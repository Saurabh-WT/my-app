pipeline {
    agent {
        label "demo"
    }
    
    stages {
        stage("Clone") {
            steps {
                // Git clone step
                git branch: "main", url: "https://github.com/Saurabh-WT/my-app.git"
            }
        }
        
        stage("Build") {
            steps {
                script {
                    // Maven build step
                    sh "mvn package"
                }
            }
        }
        
        stage("Deploy") {
            steps {
                script {
                    // SCP deploy step
                    sh "whoami"
                    sh "cp target/app.war /home/Saurabh/apache-tomcat-9.0.85/webapps"
                }
            }
        }
    }
}
